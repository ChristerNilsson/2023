<script>
	import { log } from '$lib/utils.js'
	import {stack,menu} from '$lib/stores.js'

  // export let data

	// function pop() {
	// 	if ($stack.length <= 1) return 
	// 	$stack = _.dropRight($stack)
	// 	goto(path($stack))
	// }

	// $: arr = data.slug ? data.slug.split('/') : []
	// $: log('data',data)
	// $: log('arr',arr)
	// $: log('stack',$stack)

	// $: {
	// 	if (stack.length > arr.length) $stack = _.dropRight($stack)
	// }

</script>

