// import _ from "lodash"
// import { log } from '$lib/utils.js'

// export const prerender = true

// export async function load({ params }){
// 	log('params',params)
// 	if (_.size(params) == 0) return {slug : []}
// 	if (params.slug == 'favicon.ico') return {slug : []}
// 	return {slug : params.slug}
// }